function findNeedle(haystack) {
  let n;
  for(let el of haystack) {
    if(el === "needle"){
      n = haystack.indexOf(el);
    }
  }
  return `found the needle at position ${n}`
}

OR

const findNeedle = haystack => `found the needle at position ${haystack.indexOf('needle')}`;
