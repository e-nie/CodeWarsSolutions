const reverseSeq = n => {
  const arr = [];
    for(let i = 1; i <= n; i++) {
      arr.unshift(i);
    }
  return arr;
};



const reverseSeq = n => {
  const arr = [];
    for(let i = n; i > 0; i--) {
      arr.push(i);
    }
  return arr;
};
