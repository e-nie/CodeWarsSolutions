function arrMultiply(arr){
   return String(arr[0] * arr[1]);
   
}

function arrMultiply(arr){
   return (arr[0] * arr[1]).toString();
   
}

function arrMultiply(arr){
   return arr[0] * arr[1] + '';
   
}
